<h1><%= @genre.name %> </h1>
<ul>
  <% @genre.songs.each do |song| %>
  <li>
    <a href=<%= song_path(song)%> >
      <% song.name %>
    </a>
    in
    <a href=<%= genre_path(song.genre)%> >
      <% song.artist.name %>
    </a>
  </li>
  <% end %>
</ul>
